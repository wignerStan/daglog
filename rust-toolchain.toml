# Pin the Rust toolchain for reproducible builds. Bumped to 1.88 so the
# optional `pg` feature's transitive deps (icu via sqlx/rustls) build.
[toolchain]
channel = "1.88"
components = ["clippy", "rustfmt", "llvm-tools-preview"]
